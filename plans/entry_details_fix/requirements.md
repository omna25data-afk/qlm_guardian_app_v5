# وثيقة المتطلبات (Requirements)

## 🎯 وصف الميزة (Feature Description)

إصلاح مشكلة عدم ظهور بعض البيانات الهامة بشكل صحيح وكامل في شاشة "تفاصيل القيد" (EntryDetailsScreen) في تطبيق فلاتر. البيانات المفقودة تشمل المبالغ المالية التفصيلية (مثل رسوم المصادقة، رسوم الانتقال، رسوم أخرى، مبلغ الدعم، الاستدامة، ونوع الإعفاء) بالإضافة إلى تفاصيل القيد في سجل الأمين (مثل تاريخ القيد الهجري).

### متطلبات العمل (Business Requirements)

1. يجب أن تعرض شاشة تفاصيل القيد (EntryDetailsScreen) جميع المبالغ المالية المسجلة للقيد، وهي:
    * مبلغ الرسوم الأساسي (`fee_amount`).
    * مبلغ الغرامة (`penalty_amount`).
    * مبلغ الدعم (`support_amount`).
    * مبلغ الاستدامة (`sustainability_amount`).
    * رسوم التصديق (`authentication_fee_amount`).
    * رسوم الانتقال (`transfer_fee_amount`).
    * رسوم أخرى (`other_fee_amount`).
    * المجموع الإجمالي لهذه الرسوم.
2. يجب أن تعرض الشاشة معلومات الاستثناء (إن وجدت)، وهي:
    * نوع الإعفاء (`exemption_type`).
    * سبب الاستثناء (`exemption_reason`).
3. يجب أن تعرض الشاشة تاريخ الأمين بالهجري (`guardian_hijri_date`) ضمن قسم بيانات قيد الأمين.
4. **تحديث (22 فبراير):** يجب أن تُظهر شاشة تفاصيل القيد **جميع الأعمدة** الموجودة في جدول `registry_entries` المركزي، وأيضاً **جميع الأعمدة تخصيصاً** للعقود المرتبطة حسب نوع العقد (مثل `sale_contracts`, `marriage_contracts` وغيرها) دون استثناء، لضمان استعراض البيانات بلا نواقص.

- [ ] يجب أن يستمر التطبيق في عرض بيانات العقد الإضافية (مثل اسم البائع والمشتري في عقود البيع، وما إلى ذلك) بشكل ديناميكي صحيح.

## 🚧 قيود وشروط (Constraints & Rules)

* التعديل الأساسي سيكون في جانب الـ Backend (Laravel) وتحديداً في الاستجابة المرسلة عبر `RegistryEntryResource.php`.
* يجب الحفاظ على التوافقية مع تطبيق الواجهة الأمامية (Flutter) بحيث تتطابق أسماء الحقول المضافة في الـ API مع ما يحاول التطبيق قراءته في موديل `RegistryEntrySections.dart`.
* يجب عدم التأثير سلباً على أداء واجهة برمجة التطبيقات (API).

## ✅ معايير القبول (Acceptance Criteria)

* [ ] عند فتح شاشة "تفاصيل القيد" لأي قيد من أي نوع، تظهر الحقول المالية كاملة بالقيم الصحيحة (وليس 0.00 إلا إذا كانت قيمتها الناتجة من قاعدة البيانات صفراً).
* [ ] إجمالي المبلغ المعروض في التطبيق يتطابق تماماً مع مجموع كافة الرسوم في قاعدة البيانات.
* [ ] تاريخ القيد الهجري في سجل الأمين يظهر في قسم "بيانات قيد الأمين" ولا يكون فارغاً أو مستنداً لبيانات خاطئة.
* [ ] اجتياز اختبار مسار المستخدم (User Journey Test) باستخدام `flutter_driver` للتأكد من سلامة العرض في الواجهات.
