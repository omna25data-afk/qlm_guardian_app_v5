# وثيقة التصميم المعماري - تحسين التصفية والفرز والإحصائيات

## 🔎 نظرة عامة

تحسين شاشة إدارة السجلات عبر تعديل 4 ملفات Flutter فقط (بدون تعديل الباكند). الفكرة التقنية: إضافة حقول فلترة جديدة إلى `AdminRecordBooksState`، وإرسالها كـ query params إلى API الموجودة فعلاً، مع تعديل الواجهة لإظهار/إخفاء عناصر فلترة حسب الفئة المختارة.

## 🛠 التعديلات المقترحة

### 1. طبقة الـ State Management

- **تعديل:** `lib/features/admin/presentation/providers/admin_record_books_provider.dart`
  - إضافة حقول: `periodType`, `periodYear`, `sortField`, `sortDirection`, `dateFrom`, `dateTo`
  - إضافة methods: `setPeriodFilter()`, `setSortField()`
  - تحديث `fetchBooks()` لإرسال params الجديدة
  - تحديث `copyWith()` بالحقول الجديدة

### 2. واجهة المستخدم - تبويب السجلات

- **تعديل:** `lib/features/admin/presentation/screens/tabs/record_books_tab.dart`
  - `build()`: إضافة صف فلتر الفترة الزمنية بين شريط البحث وشريط التجميع
  - `_buildGroupByBar()`: جعل الخيارات ديناميكية حسب الفئة المختارة + إضافة PopupMenu للفرز
  - `_showAdvancedFilters()`: إخفاء dropdown الأمين عند فئة غير `guardian_recording`

- **تعديل:** `lib/features/admin/presentation/widgets/premium_record_book_card.dart`
  - القسم 4 (Statistics Grid): تفصيل الإحصائيات → (موثقة / مقيدة / مسودة / مرفوضة) بدل (موثقة / قيد العمل)

### 3. واجهة المستخدم - تبويب القيود

- **تعديل:** `lib/features/admin/presentation/screens/tabs/all_entries_tab.dart`
  - إضافة فلتر فئة السجل (SegmentedButton فرعي)
  - إضافة فلتر الفترة
  - إضافة شريحة حالة "مقيدة" (registered)
  - تمرير الـ filters الجديدة إلى `AllEntriesNotifier`

## 🔄 مسار البيانات

```
المستخدم → اختيار فئة → تحديث State → إعادة fetch مع params → API ترجع بيانات مفلترة → عرض في UI
                                                          ↓
                                              params: category, period_type, year, sort_by, date_from, date_to
```
