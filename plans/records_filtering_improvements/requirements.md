# وثيقة المتطلبات: تحسين التصفية والفرز والإحصائيات في شاشة إدارة السجلات

## 🎯 وصف الميزة (Feature Description)

تحسين شامل لشاشة **إدارة السجلات** في التطبيق (تبويبات: السجلات/الدفاتر - القيود/المحررات - فحص وتفتيش). يشمل:

1. ضبط طرق الفلترة والتصفية بحسب فئة كل سجل
2. تصحيح إحصائيات العرض في بطاقات السجلات
3. إضافة فلتر الفترة الزمنية (سنوية/نصف سنوية/ربع سنوية/مخصصة)
4. تحسين شريط الفرز والتجميع ليناسب كل فئة

## 📋 متطلبات الأعمال (Business Requirements)

### أولاً: تبويب السجلات (الدفاتر) - `RecordBooksTab`

- [x] الفئات الثلاث معتمدة بالفعل: `guardian_recording` / `documentation_recording` / `documentation_final` ✅
- [ ] **REQ-01**: إضافة فلتر الفترة الزمنية مع خيارات: (سنوية - نصف سنوية - ربع سنوية - مخصصة)
  - لسجلات الأمناء (`guardian_recording`): يعتمد على `document_hijri_date` أو `document_gregorian_date` للمحررات، وإذا لم يتوفر يستخدم عمود تاريخ القيد في سجل الأمين `guardian_hijri_date`.
  بالإضافة إلى البيانات الأخرى من جدول القيود المركزي والجداول المرتبطة به.
  - لسجلات تحرير قلم التوثيق (`documentation_recording`): يعتمد على `document_hijri_date` أو `doc_hijri_date`
  - لسجلات التوثيق النهائية (`documentation_final`): يعتمد على `doc_hijri_date` / `doc_gregorian_date` (تاريخ التوثيق)
- [ ] **REQ-02**: تحسين خيارات التجميع (Group By) لتكون ملائمة لكل فئة:
  - `guardian_recording`: تجميع حسب: النوع (نوع العقد) / الأمين / السنة الهجرية
  - `documentation_recording`: تجميع حسب: النوع / السنة الهجرية
  - `documentation_final`: تجميع حسب: النوع / السنة الهجرية / الكاتب (نوع الكاتب)
- [ ] **REQ-03**: عند اختيار فئة `guardian_recording` يظهر فلتر الأمين فقط. عند اختيار الفئات الأخرى يختفي فلتر الأمين (لأن السجلات ليست مرتبطة بأمين مُعيّن)
- [ ] **REQ-04**: تصحيح إحصائيات البطاقات:
  - البطاقة تعرض حالياً: (إجمالي القيود - موثقة - قيد العمل)
  - يجب التأكد من أن الإحصائيات تتوافق مع فئة السجل:
    - `guardian_recording` → إحصائيات المحررات المقيدة في سجل الأمين (عبر `guardian_record_book_id`)
    - `documentation_recording` → إحصائيات المحررات المقيدة في سجل التوثيق (عبر `doc_record_book_id`)
    - `documentation_final` → إحصائيات المحررات الموثقة نهائياً (عبر `doc_record_book_id`)
  - **هذا يتم من الباكند** - `RecordBookResource.php` يستخدم `$this->entries()` الذي يعتمد على الفئة فعلاً ✅
  - تفصيل عرض الإحصائيات: (موثقة / مقيدة / مسودة / مرفوضة) بدل (موثقة / قيد العمل)
- [ ] **REQ-05**: فلتر **نوع السجل** (Record Book Type) - dropdown أو chips لاختيار نوع السجل
- [ ] **REQ-06**: فلتر **نوع العقد** (Contract Type) - dropdown
- [ ] **REQ-07**: فلتر **حالة السجل** (Status: نشط/مغلق/مكتمل/معلق) - chips أسفل البحث

### ثانياً: تبويب القيود (المحررات) - `AllEntriesTab`

- [ ] **REQ-08**: تحسين فلتر نوع الكاتب ليشمل: (الكل - أمين شرعي - قلم التوثيق - كاتب آخر/خارجي) ✅ موجود فعلاً
- [ ] **REQ-09**: إضافة فلتر فئة السجل (Record Book Category) لتصفية المحررات حسب نوع السجل المقيدة فيه
- [ ] **REQ-10**: إضافة فلتر الفترة الزمنية للمحررات مع نفس الخيارات (سنوية/نصف سنوية/ربع سنوية/مخصصة)
- [ ] **REQ-11**: إضافة فلتر الحالة `registered` (مقيدة في سجل الأمين) كخيار إضافي في شرائح الحالة
- [ ] **REQ-12**: فلتر **نوع العقد** (Contract Type) - dropdown في الفلاتر المتقدمة (✅ موجود فعلاً، نتأكد من عمله)
- [ ] **REQ-13**: فلتر **حالة القيد** الشامل - إضافة حالات: (موثق/قيد التدقيق/مسودة/مقيدة/مرفوض)
- [ ] **REQ-14**: فلتر **تاريخ التحرير** (date range) - يشمل جميع الأعمدة الخاصة والبديلة:
  - `document_hijri_date` / `document_gregorian_date` (تاريخ تحرير المحرر)
  - في حال عدم وجود قيمة → استخدام `guardian_hijri_date` كبديل (تاريخ قيد الأمين)
- [ ] **REQ-15**: فلتر **تاريخ التوثيق** (date range):
  - `doc_hijri_date` / `doc_gregorian_date` (تاريخ القيد في سجل التوثيق)

### ثالثاً: ترتيب/فرز القوائم

- [ ] **REQ-16**: إضافة خيار الفرز (Sort) بجانب التجميع في تبويب السجلات:
  - فرز حسب: رقم السجل / تاريخ الإنشاء / نسبة الاستخدام / عدد القيود
- [ ] **REQ-17**: فرز افتراضي للمحررات حسب: تاريخ التحرير (الأحدث أولاً)

## 🚧 قيود وشروط (Constraints & Rules)

- لا تغيير على الباكند (Backend) - كل التحسينات في Flutter فقط
- استخدام المكتبات والحزم الموجودة فعلاً في المشروع (Riverpod, Material)
- الحفاظ على تصميم UI الحالي مع تحسينه بطريقة لا تكسر التوافق
- البيانات الموجودة بالفعل في `RecordBookResource.php` كافية للإحصائيات - لا نحتاج endpoint جديدة
- API الحالية تدعم فعلاً: `status`, `contract_type_id`, `type`, `category`, `guardian_id`, `sort_by`, `date_from`, `date_to`
- فلتر الفترة الزمنية يتم تطبيقه على الجانب الخلفي (API) إذا كان متاحاً أو على جانب Flutter محلياً

## ✅ معايير القبول (Acceptance Criteria)

- [ ] عند اختيار فئة `guardian_recording` يظهر فلتر الأمين ويكون التجميع مخصصاً لسجلات الأمناء
- [ ] عند اختيار `documentation_recording` أو `documentation_final` يختفي فلتر الأمين
- [ ] إحصائيات البطاقات تعرض التفاصيل (موثقة/مقيدة/مسودة/مرفوضة) بدل مجرد (موثقة/قيد العمل)
- [ ] فلتر الفترة الزمنية يعمل بشكل صحيح لكل فئة
- [ ] فلتر نوع السجل ونوع العقد وحالة السجل تعمل في تبويب السجلات
- [ ] فلتر تاريخ التحرير وتاريخ التوثيق وحالة القيد تعمل في تبويب القيود
- [ ] خيارات التجميع تتغير ديناميكياً حسب الفئة المختارة
- [ ] الفرز يعمل بشكل صحيح في كلا التبويبين
- [ ] التطبيق يبني بدون أخطاء تحليلية (Analyzer)
- [ ] التصميم احترافي ومتجاوب (RTL-friendly)
