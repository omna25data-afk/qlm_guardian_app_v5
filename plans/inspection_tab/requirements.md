# وثيقة المتطلبات (Requirements) - تبويب فحص وتفتيش

## 🎯 وصف الميزة (Feature Description)

إضافة تبويب ثالث بعنوان **"فحص وتفتيش"** إلى شاشة "إدارة السجلات والقيود" (`AdminRecordsScreen`). يعرض هذا التبويب بيانات فحص وتفتيش السجلات (Record Book Inspections) المسحوبة من الواجهة الخلفية عبر API موجود مسبقاً (`admin/inspection/record-books`). يهدف التبويب إلى تمكين المسؤول من استعراض سجلات الفحص والتفتيش ومتابعة حالاتها وملاحظاتها.

## 📋 متطلبات الأعمال (Business Requirements)

### 1- عرض قائمة سجلات الفحص والتفتيش

- [x] عرض قائمة بجميع سجلات الفحص والتفتيش (`RecordBookInspection`) من الباكند.
- [x] كل بطاقة تعرض: اسم السجل المفحوص، تاريخ الفحص، رقم الفحص، السنة الهجرية، الربع، الحالة.
- [x] دعم البحث اللحظي في سجلات الفحص.

### 2- تفاصيل الفحص

- [x] عند الضغط على بطاقة فحص، يتم فتح شاشة تفاصيل تعرض معلومات الفحص المفصلة.
- [x] عرض الملاحظات العامة والحالة والفترة الزمنية.

### 3- استخدام البنية التحتية الموجودة

- [x] الاستفادة من `AdminInspectionNotifier` و `AdminInspectionState` الموجودين مسبقاً.
- [x] الاستفادة من `AdminRepository.getInspectionRecordBooks()` و `AdminRepository.getInspectionRecordBookDetail()`.
- [x] الاستفادة من نقاط API الموجودة: `adminInspectionRecordBooks`, `adminInspectionRecordBookDetail`.

## 🚧 قيود وشروط (Constraints & Rules)

- استخدام `Riverpod` لإدارة الحالة (Provider موجود مسبقاً).
- اتباع أنماط التصميم الموجودة في التبويبات الأخرى (`RecordBooksTab`, `AllEntriesTab`).
- عدم المساس بالتبويبات الموجودة أو تغيير سلوكها.
- ضمان التوافق مع `TabController` الحالي (تحديث `length` من 2 إلى 3).

## ✅ معايير القبول (Acceptance Criteria)

- [x] ظهور تبويب "فحص وتفتيش" كتبويب ثالث في شاشة إدارة السجلات.
- [x] عرض قائمة سجلات الفحص بنجاح من الباكند عند فتح التبويب.
- [x] دعم البحث في سجلات الفحص.
- [x] عرض تفاصيل الفحص عند الضغط على بطاقة.
- [x] خلو الكود من أخطاء التحليل النحوي.
