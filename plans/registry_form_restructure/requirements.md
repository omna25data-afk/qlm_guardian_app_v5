# وثيقة المتطلبات (Requirements)

## 🎯 وصف الميزة (Feature Description)

إعادة هيكلة تخطيط وترتيب شاشة إضافة القيود "النسخة المختصرة" (`CompactRegistryEntryScreen`) في تطبيق Flutter لتطابق التسلسل المنطقي الحديث المدعوم من الباكند (Filament)، بحيث يتم التأكد من تسلسل البيانات، منع تكرار الإدخال، وتقسيم الشاشة إلى 3 أقسام رئيسية واضحة للمستخدم.

## 📋 متطلبات الأعمال (Business Requirements)

- [ ] **القسم الأول (بيانات العقد والكاتب):**
  - دمج نوع العقد، الأنواع الفرعية مع بيانات الكاتب والمحرر.
  - إظهار قائمة اختيار سجل الأمين (فقط للموثقين من نوع "أمين شرعي") بصيغة محددة `نوع السجل + رقمه + السنة + اسم الأمين`.
  - إضافة تواريخ المحرر (هجري/ميلادي) وجلب تاريخ القيد للأمين افتراضياً منها.
- [ ] **القسم الثاني (بيانات القيد والأطراف):**
  - عرض أسماء الأطراف والتفاصيل الديناميكية للعقد.
  - إضافة "إشعار بانر" يوضّح للمستخدم أنه في حال كان القيد مُقيّد مسبقاً لدى الأمين، فعليه استكمال بيانات القسم الثالث (التوثيق المالي) فقط.
- [ ] **القسم الثالث (البيانات المالية وسجل التوثيق):**
  - تخصيص قسم لبيانات التوثيق (سجل المحكمة، تاريخ التوثيق، البوكس...إلخ).
  - قسم فرعي مخصص للضريبة (المبلغ ورقم السند).
  - قسم فرعي مخصص للزكاة (المبلغ ورقم السند).
  - قسم فرعي لديناميكية الرسوم: زر الإعفاء، غرامات التأخير، المجاميع (Total).
  - قسم فرعي يظهر "تفاصيل توزيع الرسوم" (للقراءة فقط) بشكل قابل للطي.

## 🚧 قيود وشروط (Constraints & Rules)

- التعديل يقتصر على واجهة المستخدم (UI/Frontend) في Flutter وملفات مزود الحالة (`AddRegistryEntryProvider`).
- يجب الالتزام بطريقة كتابة واجهات التطبيق (استخدام `FormSectionCard`).
- يجب ضمان عدم تعطل الـ `submitEntry` ومطابقة الحقول للحزمة المرسلة (JSON).

## ✅ معايير القبول (Acceptance Criteria)

- [ ] عند تشغيل التطبيق واختيار عقد، يظهر التخطيط الجديد المكون من 3 أقسام.
- [ ] عند اختيار الكاتب كـ "أمين شرعي" تظهر القائمة المنسدلة الذكية لدفتر الأمين بالصيغة الصحيحة.
- [ ] عند تفعيل "زر الإعفاء" تتصفر الرسوم وتختفي الحقول التابعة لها وتظهر خانة "سبب الإعفاء".
- [ ] الحسابات المعروضة في "تفاصيل التوزيع" تكون Read-Only ومسدلة (Collapsible) بشكل افتراضي.
