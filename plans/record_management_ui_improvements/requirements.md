# وثيقة المتطلبات (Requirements)

## 🎯 وصف الميزة (Feature Description)

تحسين واجهة المستخدم الخاصة بإدارة السجلات والقيود في التطبيق (تبويبات: السجلات، القيود، التفتيش) عن طريق إضافة أيقونات متقدمة للفرز، التصفية، العرض والتجميع، بالإضافة إلى تحسين تصميم بطاقات السجلات والقيود لتعرض كافة التفاصيل كما في النموذج الأساسي، مع إضافة أيقونة لتعديل القيد باستخدام نموذج الإضافة المقسم.

## 📋 متطلبات الأعمال (Business Requirements)

- [ ] إضافة أيقونات (فرز، تصفية، عرض، تجميع) في شريط الأدوات العلوي (أو بجانب حقل البحث) في تبويب السجلات (`record_books_tab.dart`).
- [ ] إضافة أيقونات (فرز، تصفية، عرض، تجميع) في تبويب القيود (`all_entries_tab.dart`).
- [ ] إضافة أيقونات (فرز، تصفية، عرض، تجميع) في تبويب التفتيش (`inspection_tab.dart`).
- [ ] تحسين تصميم بطاقة السجل (`PremiumRecordBookCard`) لتشمل كافة التفاصيل التي تظهر في النموذج (مثل اسم الأمين، المحكمة، التواريخ، النماذج، إلخ).
- [ ] تحسين تصميم بطاقة القيد (`PremiumEntryCard`) لتشمل كافة التفاصيل الخاصة بالقيد (بيانات المحرر، بيانات الوثيقة، البيانات المالية).
- [ ] توفير زر/أيقونة "تعديل" في بطاقة القيد، للتمكن من فتح واجهة تعديل القيد (`CompactRegistryEntryScreen` أو ما يماثلها) وتمرير البيانات الحالية لتسهيل عملية التحديث.

## 🚧 قيود وشروط (Constraints & Rules)

- الالتزام بنظام التصميم الحالي للتطبيق (ألوان، حواف، خطوط).
- أيقونات العرض والتجميع يجب أن تكون واضحة للمستخدم (Grid vs List، Group by Status/Type).
- استخدام الأيقونات القياسية من مكتبات التطبيق المعتمدة (مثل Iconsax أو ما يماثلها إن وجدت).
- عدم كسر الوظائف الحالية للتصفية المدعومة بواسطة الـ Providers المعنية لكل تبويب.

## ✅ معايير القبول (Acceptance Criteria)

- [ ] عند فتح أي من التبويبات الخاصة بإدارة السجلات، تظهر الأيقونات المتقدمة بشكل متناسق مع الواجهة ويمكن النقر عليها.
- [ ] بطاقات السجلات تعرض المزيد من التفاصيل المرتبطة بشكل واضح ومرتب.
- [ ] بطاقات القيود تعرض بيانات محرري العقود والأطراف والرسوم المالية بوضوح.
- [ ] النقر على أيقونة "تعديل" في بطاقة القيد يفتح شاشة الإضافة/التعديل المقسمة، ومملوءة ببيانات القيد الحالي.
- [ ] مرور اختبار User Journey باستخدام Flutter Driver الذي يحاكي تصفح هذه التبويبات واختبار الواجهة.
