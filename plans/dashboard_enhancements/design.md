# وثيقة التصميم المعماري (Design)

## 🔎 نظرة عامة (Overview)

تحويل `ReportsDashboardScreen` الحالية إلى شاشة تحتوي على مساحة علوية مبنية بـ `DefaultTabController` تدير تبويبين (إحصائيات وتقارير).

- التبويب الأول (الإحصائيات) سيعرض محتوى `GuardianStatisticsScreen`.
- التبويب الثاني (التقارير) سيعرض المحتوى الحالي لـ `ReportsDashboardScreen` (خيارات التصفية وقائمة التقارير مثل `FeesReportScreen`).

## 🛠 التعديلات المقترحة (Proposed Changes)

### 1. واجهة المستخدم (UI / Frontend)

- **ملف جديد/مُعاد تصميمه:** `lib/features/reports/presentation/screens/main_dashboard_screen.dart`
  - سيتم إنشاء هذه الشاشة لتكون الحاوية الأساسية (Host) التي تدمج كلاً من الشاشتين الحاليتين.
  - ستستخدم `DefaultTabController` بتبويبين `TabBar` (بأيقونات ونصوص تعبر عن الإحصائيات والتقارير).
  - داخل `TabBarView`، سنقوم بوضع `GuardianStatisticsScreen()` في الصفحة الأولى.
  - سنقوم بفصل جزء عرض التقارير من `ReportsDashboardScreen` ووضعه كعنصر (Widget) في الصفحة الثانية واسمه `ReportsTabWidget()`.

### 2. تعديلات على ملفات قائمة

- **`lib/features/reports/presentation/screens/reports_dashboard_screen.dart`:**
  - سيتم إلحاق محتواه ليصبح ويدجت داخلي أو يتم الاعتماد على شاشة `main_dashboard_screen.dart` كبديل يوجّه إليه الروتر (Router).
  - سيتم تبسيط جزء عرض القائمة (List/Grid) ليتناسب مع كونه جزءاً من Tab بدلاً من شاشة كاملة تحتكر الـ AppBar.
- **`lib/features/reports/presentation/screens/guardian_statistics_screen.dart`:**
  - إزالة الـ `Scaffold` والـ `AppBar` الداخلي ليصبح مجرد قائمة (Column/ListView) يمكن حشوها داخل الـ TabView بسلاسة.

### 3. التوجيه (Routing)

- تعديل مسار `'/reports'` في إعدادات مسارات التطبيق (GoRouter أو Navigator) ليوجه المستخدم إلى `MainDashboardScreen` الجديدة بدلاً من الشاشة القديمة.

## 🔄 كيف سيتم التحقق (Verification Plan)

- **اختبار يدوي:** سأطلب منك كـ(مطور/مستخدم) تشغيل التطبيق بالروتين الطبيعي (`F5` أو `flutter run`) وفتح قسم التقارير من القائمة الجانبية أو الرئيسية. يجب التأكد من ظهور التبويبات وإمكانية التنقل بينها دون كسور في الواجهة وأن الأزرار وفلاتر البحث تعمل في القسمين بانسجام مع الـ Providers الحالية.
