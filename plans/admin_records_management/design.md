# وثيقة التصميم المعماري (Design Document)

## 🔎 نظرة عامة (Overview)

تهدف هذه الميزة إلى إعادة تصميم وتنظيم شاشة إدارة السجلات والقيود الخاصة بمدير النظام (رئيس قلم التوثيق). ستعتمد الواجهة الجديدة على الفصل الواضح بين إدارة **دفاتر السجلات (Record Books)** وإدارة **المحررات/القيود (Registry Entries)**. سيتم استخدام `Riverpod` لإدارة حالة التبويبات والبيانات المجلوبة، مع توفير آليات فرز وبحث متقدمة. بالإضافة إلى ذلك، سيتم بناء هيكل جديد ومتقدم لـ `AdminAddEntryScreen` (نموذج إضافة القيد) يعتمد المنطق المتسلسل متعدد الخطوات (Stepper أو أقسام قابلة للطي) لمنع تكرار الإدخال وتسهيل عملية جلب البيانات من سجلات الأمناء إن وجدت.

## 🛠 التعديلات المقترحة (Proposed Changes)

### 1. طبقة قواعد البيانات والنماذج (Database & Models)

- **لا تغييرات جذرية على الجداول الأساسية**: النماذج الحالية (`RecordBook`, `RegistryEntry`) تدعم كافة المتطلبات.
- سيتم الاعتماد على الـ Enums الموجودة مسبقاً في جلب التصنيفات (أنواع الكتاب، حالات القيد، وأنواع السجلات).

### 2. منطق الأعمال ومزودو الخدمة (Business Logic / Controllers / Providers)

- **تعديلات على مزودي الحالة (Providers):**
  - **`admin_record_books_provider.dart`**: تثبيت وتوسيع فلاتر السجلات (سجلاتي، سجلات الأمناء المفلترة، سجلات خارجية) وتجميعها داخل حاويات (Group by).
  - **`all_entries_provider.dart` (جديد/معدل)**: إنشاء StateNotifier يدير حالة قائمة القيود بشكل مستقل، مع دعم فلترة (قيودي، قيود الأمناء الموثقة/قيد التوثيق، قيود الآخرين) ودعم البحث المتطور.
  - **نموذج "إضافة قيد" Providers**: إنشاء مزود جديد لإدارة حالة النموذج المكون من ثلاثة أقسام (نوع الكاتب وبياناته، بيانات العقد المتغيرة بحسب النوع، والبيانات المالية/التوثيق). هذا المزود سيهتم بتوليد أسماء الدفاتر ديناميكياً بناءً على المستخدم، والتاريخ، والنوع الدفتر كما هو مشروح في المتطلبات.

### 3. واجهة المستخدم (UI / Frontend)

- **ملف `admin_records_screen.dart`**:
  - التأكد من احتوائه على `TabBar` علوي بتبويين أساسيين فقط (السجلات، القيود).

- **ملف `tabs/record_books_tab.dart`**:
  - إضافة تبويبات فرعية (Filter Chips) لفرز الدفاتر (سجلاتي، سجلات الأمناء، سجلات أخرى).
  - تجميع دفاتر الأمناء داخل حاويات (Expandable Panels أو ListViews مجمعة).

- **ملف `tabs/all_entries_tab.dart`**:
  - إضافة تبويبات فرعية (قيودي، قيود الأمناء، قيود أخرى).
  - التركيز على إضافة زر "توثيق عقد" مخصص يستدعي قائمة منبثقة أو شاشة فرعية للعقود المعلقة (Pending Documentation) لتوثيقها.

- **ملف الشاشة الجديد لنموذج إضافة/توثيق قيد `admin_add_entry_screen.dart`**:
  - إعادة بناء الشاشة كلياً باستخدام `Stepper` أو تصميم مقسم وواضح يعكس الأقسام الثلاثة:
    1. **القسم الأول**: بيانات الكاتب والعقد المرتبطة، اختيار الدفتر الخاص بالكاتب (توليد الاسم ديناميكياً).
    2. **القسم الثاني**: نموذج العقد الديناميكي المستورد من سجل الأمين (إن وجد) أو إدخال جديد.
    3. **القسم الثالث**: بيانات التوثيق في المحكمة، اختيار/تعبئة دفتر التوثيق (توليد الاسم ديناميكياً)، معمارية تفصيلية للرسوم المالية (الإعفاءات، ضريبة، زكاة، رسوم توثيق وغرامات، ودعم استدامة) بواجهة قابلة للطي (ExpansionTile).

## 🔄 مسار البيانات (Data Flow)

1. **دخول الشاشة**: يجلب النظام قائمة السجلات أو القيود عبر `AdminRepository` بناءً على التبويب المفتوح والفلتر الافتراضي.
2. **إضافة قيد**:
   - يختار الآدمن "إضافة قيد".
   - يُفتح نموذج مقسم ومترابط.
   - بمجرد تغيير الكاتب (إلى أمين مثلاً) وتحديد النوع والتاريخ، يقوم التطبيق بتوليد ومطابقة قائمة دفاتر الأمين هذا (الاسم المركب) لتعرض كقائمة منسدلة.
   - نفس الأمر للقسم الثالث، حيث يتولد اسم سلسلة دفتر التوثيق بناءً على التاريخ ونوع المعاملة تلقائياً.
3. **التوثيق**:
   - إذا اختار الآدمن "توثيق عقد"، تُعرض العقود الجاهزة.
   - عند الاختيار، تُسحب بيانات العقد وتملأ القسم الثاني تلقائياً.
   - يُكمل الآدمن تعبئة القسم الثالث (بيانات المال والتوثيق البحتة) ويحفظ.
4. **الحفظ النهائي**: يقوم الـ API (Laravel) باستلام هيكل الـ `RegistryEntry` ومعالجة التفاصيل الدقيقة داخلياً وفقاً للكمية المعطاة للرسوم وحالة القيد الجديدة (Documented).
