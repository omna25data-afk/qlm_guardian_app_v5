# مسار تصحيح شاشة إدارة السجلات والقيود

(Records & Entries Correction Path)

## 🎯 وصف الميزة (Feature Description)

إضافة إمكانية "مسار تصحيح" (Correction Path) لتمكين المستخدم (مدير النظام أو صاحب الصلاحية) من تعديل وتصحيح تفاصيل السجلات (الدفاتر - Record Books) والقيود (العقود والمحررات - Registry Entries) عبر شاشة `AdminRecordsScreen`. الهدف هو معالجة أي أخطاء في الإدخال (مثل أخطاء في أسماء الأطراف، التواريخ، الرسوم، أو أرقام القيود) وتحديث بيانات قاعدة البيانات المركزية مباشرة.

## 📋 متطلبات الأعمال (Business Requirements)

- [ ] **عرض التفاصيل الدقيقة:** عند النقر على (سجل RecordBook) أو (قيد RegistryEntry) في القوائم، يتم فتح شاشة/نافذة تعرض تفاصيل العنصر بناءً على هيكلية قاعدة البيانات الموثقة.
- [ ] **تصحيح القيود (Registry Entries):** إمكانية تعديل الحقول الأساسية للقيد، مثل:
  - الأطراف (اسم الطرف الأول والثاني).
  - التواريخ (تاريخ التحرير، تاريخ القيد).
  - البيانات المالية (مبلغ الرسوم، الغرامات، رسوم المصادقة/الانتقال، الاستدامة والدعم).
  - أرقام القيد (معرف السجل، رقم الصفحة، رقم القيد المتسلسل).
- [ ] **تصحيح السجلات (Record Books):** إمكانية تعديل الحقول الأساسية للسجل (مثل رقم السجل، السنة، حالة السجل).
- [ ] **حفظ التعديلات (Backend Sync):** إرسال البيانات المحدثة عبر API (PUT/PATCH request) لتحديث قاعدة بيانات `custom_filment` المركزية.
- [ ] **تحديث واجهة المستخدم (State Management):** التحديث الفوري لحالة التطبيق باستخدام `Riverpod` لتعكس التعديلات في قوائم شاشة الإدارة دون الحاجة لإعادة تحميل كاملة.

## 🚧 قيود وشروط (Constraints & Rules)

- الالتزام بالمصطلحات المعتمدة (السجل، القيد، المحرر، الأمين، قلم التوثيق).
- مراعاة أنواع القيود وحالاتها عند التعديل (مثلاً مسودة، مقيد لدى الأمين، موثق).
- استخدام بنية `Reactive Forms` إن أمكن لتسهيل عملية التحقق من صحة البيانات (Validation) قبل الإرسال.
- إدارة الأخطاء بدقة في حال فشل الاتصال بالخادم.
- توافق التصميم مع واجهة `AdminRecordsScreen` الحالية وهوية التطبيق `AppTheme`.

## ✅ معايير القبول (Acceptance Criteria)

- [ ] يستطيع المستخدم اختيار قيد (Registry Entry) محدد وتعديل مبلغه المالي أو اسم أحد أطرافه، ويتم حفظه بنجاح وانعكاسه في القائمة.
- [ ] يستطيع المستخدم اختيار سجل (Record Book) محدد وتعديل بياناته الأساسية وحفظه بنجاح.
- [ ] يتم عرض رسالة نجاح واضحة للمستخدم (Snackbar) عند إتمام عملية التصحيح.
- [ ] يتم عرض رسائل خطأ دقيقة في حالة إرجاع الـ API لأي أخطاء تحقق (Validation Errors) أو أخطاء سيرفر (500).
